name: Build REDMI K30 picasso (MIUI 13 A12)

on:
  workflow_call:
  workflow_dispatch:

env:
  # Device env
  DEVICE_NAME : "redmi_k30_picasso"
  DEVICE_CODENAME : "picasso"

  EXTRA_CMDS: "LD=ld.lld LLVM=1 LLVM_IAS=1 CONFIG_COMPAT_VDSO=n"
  CUSTOM_CMDS: "CLANG_TRIPLE=aarch64-linux-gnu- CONFIG_COMPAT_VDSO=n"

  KERNEL_SOURCE : "https://github.com/Kiz9178/android_kernel_xiaomi_sm7250.git"
  KERNEL_BRANCH : "16-susfs-miui"

  CLANG_SOURCE: ""
  CLANG_BRANCH : ""

  
  # 启用 GCC
  GCC_GNU: true
  GCC_64_SOURCE: "https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9.git"
  GCC_64_BRANCH: "android12L-release"
  GCC_32_SOURCE: "https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9.git"
  GCC_32_BRANCH: "android12L-release"
  

  DEFCONFIG_SOURCE: ""
  DEFCONFIG_NAME : "vendor/xiaomi/picasso.config"
  DEFCONFIG_ORIGIN_IMAGE: ""
  DEFCONFIG_FROM_BOOT: false

  KERNELSU_SOURCE : "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/refs/heads/main/kernel/setup.sh"
  KERNELSU_BRANCH : "v4.1.0"

  KERNELSU_AUTO_GET : "true"
  KERNELSU_AUTO_FORK : "resukisu"

  SUSFS_ENABLE : true
  SUSFS_FIXED : true
  SUSFS_UPDATE : true

  AK3_SOURCE : "https://github.com/osm0sis/AnyKernel3.git"
  AK3_BRANCH : "master"

  BOOT_SOURCE : ""

  NEED_DTBO : false
  NEED_SAFE_DTBO : false

  ROM_TEXT : "rom_android"

  PYTHON_VERSION: "3"

  PACK_METHOD: "Anykernel3"
  PACK_KMODULES: "false"

  KERNELSU_METHOD: "shell"

  PATCHES_SOURCE: "Kiz9178/NonGKI_Kernel_Build_2nd"
  PATCHES_BRANCH: "sample"

  SUSFS_FOLDER_FIXED: "Patches/Patch"
  SUSFS_PATCH_FIXED: "susfs_patch_to_4.19"

  REKERNEL_ENABLE: "false"

  HOOK_METHOD: "syscall"
  HOOK_OLDER: "false"

  KERNEL_PATCH: "false"

  KPM_ENABLE: "true"
  KPM_INLINE: "false"
  KPM_KPATCH_NEXT: "false"
  KPM_PATCH_SOURCE: "https://github.com/SukiSU-Ultra/SukiSU_KernelPatch_patch/releases/download/0.13.0/patch_linux"

  BASEBAND_GUARD_ENABLE: "false"
  BASEBAND_GUARD_BOOT: "false"

  HIDE_STUFF: "false"

  GENERATE_DTB: "false"
  GENERATE_CHIP: "qcom"

  BUILD_DEBUGGER: "false"
  SKIP_PATCH: "false"

  BUILD_OTHER_CONFIG: "false"
  MERGE_CONFIG_FILES: "vendor/debugfs.config,vendor/lahaina_QGKI.config"

  FREE_MORE_SPACE: "false"

jobs:
  build-kernel:
    name: redmi K30 picasso (MIUI 13 A12)
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"

    steps:
      - name: ⏰ Checkout repository
        uses: actions/checkout@v6

      - name: ⌛️ Compilation environment preparation
        uses: ./.github/workflows/build-env

      - name: ⚙️ Get neccssary tools
        uses: ./.github/workflows/build-ready

      - name: 🔧 Fix compat_uptr_t and header issues
        run: |
          cd $GITHUB_WORKSPACE/device_kernel
          
          # 1. 确保 CONFIG_COMPAT 已启用
          if [ -f "arch/arm64/configs/${{ env.DEFCONFIG_NAME }}" ]; then
            echo "CONFIG_COMPAT=y" >> arch/arm64/configs/${{ env.DEFCONFIG_NAME }}
            echo "CONFIG_COMPAT_32BIT_TIME=y" >> arch/arm64/configs/${{ env.DEFCONFIG_NAME }}
          fi
          
          # 2. 修复 compat_uptr_t 类型定义
          cat >> include/linux/types.h << 'EOF'
          #ifndef compat_uptr_t
          #ifdef CONFIG_COMPAT
          typedef u32 compat_uptr_t;
          #else
          typedef unsigned long compat_uptr_t;
          #endif
          #endif
          EOF
          
          # 3. 修复 filter.h
          if grep -q "compat_uptr_t" include/linux/filter.h; then
            sed -i '/^#include <linux\/types.h>/a #ifndef compat_uptr_t\ntypedef u32 compat_uptr_t;\n#endif\n' include/linux/filter.h
          fi
          
          # 4. 修复 millet.h（如果存在）
          if [ -f "include/linux/millet.h" ]; then
            sed -i '/^#include <linux\/types.h>/a #ifndef compat_uptr_t\ntypedef u32 compat_uptr_t;\n#endif\n' include/linux/millet.h
            # 注释掉未使用的变量
            sed -i 's/static const char \*NAME_ARRAY/__maybe_unused static const char *NAME_ARRAY/' include/linux/millet.h
          fi
          
          # 5. 修复可能缺失的其他头文件
          echo "compat_uptr_t fix applied"

      - name: 🪝 Patch Kernel of SUSFS
        uses: ./.github/workflows/patch-susfs

      - name: 🔗 Patch for no-kprobe
        uses: ./.github/workflows/patch-no-kprobe

      - name: 🪛 Extra Kernel Options
        run: |
          cd $GITHUB_WORKSPACE/device_kernel
          
          # 额外配置修复
          scripts/config --enable CONFIG_COMPAT
          scripts/config --enable CONFIG_COMPAT_32BIT_TIME
          scripts/config --disable CONFIG_COMPAT_VDSO
          
          echo "Extra kernel options applied"

      - name: 🧰 Patch Kernel of Re:Kernel
        uses: ./.github/workflows/patch-rekernel

      - name: 🔒 Patch Kernel of Baseband Guard
        uses: ./.github/workflows/patch-baseband-guard

      - name: 🔧 Patch Hide Stuff
        uses: ./.github/workflows/patch-hide-stuff

      - name: 🚀 Build Process
        uses: ./.github/workflows/build-process

      - name: 🔩 Pack Process
        uses: ./.github/workflows/pack-process

      - name: ✏️ Upload Artifacts
        uses: ./.github/workflows/upload-files
